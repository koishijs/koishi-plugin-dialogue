commands:
  teach:
    options:
      setPred: 设置前置问题
      addPred: 添加前置问题
      setSucc: 设置后置问题
      addSucc: 添加后置问题
      createSuccessor: 创建并添加后继问答
      successorTimeout: 设置允许触发后继的时间
      context.true: 允许后继问答被任何人触发
      context.false: 后继问答只能被同一人触发

    messages:
      flowgraph:
        not-found: 没有搜索到任何问答。
        detail:
          context-mode: 后继问答可以被上下文内任何人触发
          timeout: 可触发后继时间：{successorTimeout | time}
          predecessors: 前置问答：
          successors: 后继问答：
        abstract:
          has-pred: 存在前置
          context-mode: 上下文后置
